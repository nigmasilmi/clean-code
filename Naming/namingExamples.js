// Variables

const userName = "Jane";
const PI_VALUE = 3.1416;

// Variables as booleans

const isLoggedIn = true;
const userIsActive = false;
const isSaved = false;

// Functions

const saveData = () => {
  console.log("saving data in DB");
};

const findMarginValue = (totalWidth, contentWidth) => {
  return totalWidth - contentWidth;
};

// Classes

class User {
  constructor() {}
  //...etc
}

class RequestBody {
  constructor() {}
  //...etc
}
